{
  "format": {
    "day": {
      "key": "$tc(format.day.key)",
      "value": "{{start:date}}"
    },
    "start": {
      "key": "$tc(format.start.key)",
      "value": "{{start:time}}",
      "commandResponse": "$tc(format.start.commandResponse) {{start:time}}"
    },
    "stop": {
      "key": "$tc(format.stop.key.set)",
      "value": "{{finish:time}}",
      "commandResponse": "$tc(format.stop.commandResponse) {{time}}"
    },
    "stop-unset": {
      "key": "$tc(format.stop.key.unset)",
      "value": "{{time}}"
    },
    "rest": {
      "key": "$tc(format.break.key.type)({{iterator}})   [{{time}}]",
      "value": "{{duration}} $tc(format.break.value.duration)"
    },
    "workDuration": {
      "key": "$tc(format.workDuration.key)",
      "value": "{{duration}}"
    },
    "breakDuration": {
      "key": "$tc(format.breakDuration.key)",
      "value": "{{duration}}"
    },
    "notes": {
      "key": "$tc(format.note.key.type)",
      "value": "{{value}}"
    },
    "worklogs": {
      "key": "$tc(format.worklog.key.type)({{iterator}}) [{{time}}]",
      "value": "{{duration}} {{key}} {{value}}",
      "commandResponse": "$tc(format.worklog.commandResponse)"
    }
  },
  "minuteRounding": 5,
  "maxWorklogDuration": 120,
  "workDuration": 8,
  "language": "en"
}
